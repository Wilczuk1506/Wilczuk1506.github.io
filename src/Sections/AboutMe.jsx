const AboutMe = ({sectionRef}) => {
    return (
        <section ref={sectionRef} className="flex flex-col items-center justify-center h-screen snap-start bg-blue-500">

        </section>
    );
}

export default AboutMe;